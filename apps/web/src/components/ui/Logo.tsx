import { nodeIcons } from '../../lib/nodeIcons';

const Logo = () => {
  return (
    <div>
      <img src={nodeIcons["Workflow"]} className="h-5 w-5" alt="Workflow" />
    </div>
  );
};

export default Logo;
